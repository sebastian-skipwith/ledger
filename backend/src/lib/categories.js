// Shared spending taxonomy used by the AI categorizer, the rules engine, and
// the MCP write tools so every category source agrees on one set of buckets.
const CATS = [
  'Groceries', 'Dining', 'Transport', 'Shopping', 'Utilities', 'Housing',
  'Entertainment', 'Health', 'Travel', 'Subscriptions', 'Income', 'Transfer', 'Other',
];

module.exports = { CATS };
